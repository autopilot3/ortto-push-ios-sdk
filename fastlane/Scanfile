scheme("OrttoSDK-Package")

devices([
  "iPhone 8"
])

output_types("html,junit")
output_files("test-report.html,test-report.xml")
output_directory(".")

code_coverage(false)
result_bundle(true) # required to get code coverage report 
derived_data_path(".build/derivedData")

skip_build(true) # speed up testing 

package_path("./")

xcargs("-parallel-testing-enabled YES -parallel-testing-worker-count 4")

# Use a lightweight simulator configuration
only_testing(["OrttoSDKTests"])