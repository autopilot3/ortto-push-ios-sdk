scheme("OrttoSDK-Package")

devices([
  "iPhone 7 (15.5)"
])

output_types("html")
output_files("test-report.html")

output_directory("./build")
disable_slide_to_type(true)

code_coverage(false)
result_bundle(true)

skip_build(true)

package_path("./")