.DS_Store
build/
*.xcuserstate

# The SDK repo's root .gitignore excludes *.xcodeproj (it is a Swift package).
# The demo ships its Xcode project, so re-include it here.
!PushDemo.xcodeproj

# Local demo credentials and provider configuration.
PushDemo/Support/OrttoEnvironment.swift
PushDemo/GoogleService-Info.plist
